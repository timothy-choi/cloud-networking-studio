# Backend API base path or URL (no trailing slash).
# For local dev with `npm run dev`, default is /api — Vite proxies that to FastAPI on port 8000.
# Override only if you need a direct URL (e.g. http://127.0.0.1:8000).
# VITE_API_BASE_URL=/api
