# AWS docker-vm template (coming soon)

Step 57D implements real Terraform validate/plan for GCP only.

AWS `docker-vm` support will be added in a follow-up release. The backend rejects
AWS docker-vm validate/plan requests with a clear "coming soon" message.
